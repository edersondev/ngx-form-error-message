import { NgxErrorMessageDirective } from './ngx-error-message.directive';

describe('NgxErrorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxErrorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
